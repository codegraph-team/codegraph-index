apply plugin: 'distribution'
apply plugin: 'maven-publish'

ext {
    distribution = [:]
    distribution.versions = [:]
}

distribution.versions = [
    'codegraph-client'       : '0.0.1',
    'codegraph-ant'          : '0.0.1',
    'codegraph-java'         : '0.0.1',
]
