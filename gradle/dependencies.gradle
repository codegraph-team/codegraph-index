ext {
    versions = [:]
    libraries = [:]
}

versions.spring = [
    core: '4.2.7.RELEASE',
]

libraries.spring = [
    base: [
        [ group: 'org.springframework', name: 'spring-core', version: versions.spring.core ],
        [ group: 'org.springframework', name: 'spring-context', version: versions.spring.core ],
    ],

    test: [
        [ group: 'org.springframework', name: 'spring-test', version: versions.spring.core ]
    ]
]

libraries.unitTesting = [
    [ group: "junit", name: "junit", version: "4.12" ],
    [ group: "org.mockito", name: "mockito-core", version: "1.10.19" ],
    [ group: "org.hamcrest", name: "hamcrest-core", version: "1.3" ],
]

